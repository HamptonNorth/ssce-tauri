# SSCE Desktop Environment Configuration
#
# NOTE: Most settings (colours, symbols, tool defaults, file paths) are now in:
#   - src/config/defaults.json (bundled defaults)
#   - ~/.config/ssce-desktop/defaults.json (user customizations via Settings UI)
#
# This file is only for development/build settings.

# =============================================================================
# DEVELOPMENT
# =============================================================================
# Show build timestamp in window title and footer (true/false)
# Useful for verifying you're testing the latest build during development
SHOW_BUILD_TIMESTAMP=false
