# SSCE Desktop Environment Configuration
# Copy this file to .env and customise as needed
# cp .env.sample .env
#
# NOTE: UI settings (colours, symbols, tool defaults) are in src/config/defaults.json
# This file is for environment-specific settings only (paths, directories)

# =============================================================================
# FILE PATHS
# =============================================================================
# Default directory for Open Image dialog
# Supports ~ for home directory expansion
# Examples: ~/Pictures/Screenshots, /home/username/project/images
DEFAULT_PATH_IMAGE_LOAD=~/Pictures

# Default directory for Save As dialog (leave empty to use image source directory)
DEFAULT_PATH_IMAGE_SAVE=

# =============================================================================
# DEVELOPMENT
# =============================================================================
# Show git commit hash in window title (true/false)
# Useful for verifying which version is running during development
SHOW_GIT_HASH=false
